# Spec 009 validation — 2026-09-09

This is the pre-successor acceptance record. The current typed source is bound by evolution minimum seed-2; seed-1 and its bound source/schema/contract/vector bytes remain preserved. Current gate status is recorded in the full workspace at `specs/003-independent-conformance-lab/implementation-2026-09-09-successor-ctk.md`; that historical successor record is outside the runtime source distribution.

**Result:** the current reference implementation passes its composite source gate, builds a wheel and
source distribution, and replays the public evolution tests from a fresh wheel installation outside
the checkout. This closes T009-011 for the bounded minimum profile. The accompanying
[current portfolio matrix](SPEC-PORTFOLIO-2026-09-09.md) closes T009-012.

This report is an additive validation record. It does not modify or re-sign the frozen
[`oac.evolution.minimum/v0.1-seed-1` coordinate](../../experiments/evolution-minimum/v0.1-seed-1/evidence-manifest.json).
In particular, the old `tasks.md` contains historical unchecked boxes for T009-011/012 and is itself
inside that coordinate's contract closure. Its original bytes remain evidence of the earlier state;
this report records the later execution.

## Source and environment

- Source: the active, uncommitted OAC working tree over
  `e301952ae08986e6398a636f04f883036bc88c8d`, copied to an isolated temporary directory.
- Python: CPython 3.12.13 on macOS, using `uv sync --all-extras` for the complete development extras.
- No runtime implementation, frozen schema, test fixture, manifest, or historical validation report
  was changed to obtain these results.
- The added `scripts/verify_installed_evolution.py` is operator test tooling. It is outside the frozen
  contract/source closure and does not change OAC wire data or execution authority.
- The source copy is **not a clean Git archive**. The current working tree contains preexisting
  uncommitted and untracked implementation material. No commit or external publication was made.

## Executed gates

| Gate | Result | Evidence |
|---|---|---|
| `make check` | PASS; 480 tests, 90.27% line coverage; composite command completed in 175.36 s | Historical full-workspace record: `运行记录/20260909-enterprise-completion/oac-make-check.log` (not included in the runtime source distribution) |
| Schema, benchmark, CTK, Go test/vet, Supplier and Plan parity, exact source-rule mutation, frozen evolution closure | PASS inside the composite gate | Same full log; historical coordinates retained |
| Spec 007 lowering tests | 46 passed inside the composite gate | Same full log |
| `uv build` | PASS; wheel and source distribution produced | Historical full-workspace records: `运行记录/20260909-enterprise-completion/oac-build/` and `运行记录/20260909-enterprise-completion/oac-validation-summary.json` (not included in the runtime source distribution) |
| Fresh installed wheel: `tests/test_evolution.py` | 7 passed, `-W error`, isolated Python import | Historical full-workspace record: `运行记录/20260909-enterprise-completion/oac-installed-evolution.json` (not included in the runtime source distribution) |
| Fresh installed wheel: packaged TCK | 33 passed, 0 failed, loaded outside checkout | Same installed receipt |
| Installed evolution reason registry | Exact JSON equality with the frozen source schema | Same installed receipt |

Built wheel: `oac_contract-0.3.0a0-py3-none-any.whl`.

```text
sha256:aead48d80e921ddd8ea9505e4723d716e35ac351bb4237b7a88ad38a83e7e13b
```

The seven evolution tests cover the public positive/UNKNOWN/completed-but-rejected examples, root
and digest binding, mutation inventory, authority and self-certification rejection, and governed
successor lineage. They are the existing bounded public tests, not a newly invented production
qualification suite.

## Repeat the installed gate

From a development environment with the locked extras and native `uv` available:

```sh
uv sync --all-extras
make check
uv build --out-dir /tmp/oac-spec009-build
uv run python scripts/verify_installed_evolution.py \
  --wheel /tmp/oac-spec009-build/oac_contract-0.3.0a0-py3-none-any.whl \
  --output /tmp/oac-spec009-installed-replay.json
```

Choose new output paths for each run. The replay creates a fresh virtual environment, installs the
selected wheel and test tooling, copies only the enumerated public test inputs to a separate working
directory, checks that `oac` imports from the new installation, and invokes Python with `-I`. It
compares the installed registry and records material/wheel hashes. Temporary environments are removed
when the command exits; their former paths in the receipt describe observed provenance, not retained
deliverables. Installation may fetch dependencies; the OAC verification itself performs no target
effects.

## Claim boundary

The result demonstrates reproducible bounded behavior of the same reference implementation from
source and a built wheel. Hashes and logs are local execution records, not independent signed build
attestation. The package version alone is insufficient identity; use the wheel digest and material
ledger.

This run does not establish organizational independence, clean Git archive reproducibility, complete
Supplier Profile coverage, Human Ground Truth, enterprise outcome effectiveness, production readiness,
automatic source admission, autonomous promotion, or runtime authorization. The zero-effect contract
remains in force. Broader future tasks keep their original evidence requirements.
